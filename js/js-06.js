let input = prompt("введите число");
const numbers = [];
let total = 0;
do {
  input = Number(input);
  numbers.push(input);
  for (number of numbers) {
    total += number;
  }
} while (input);

console.log(total);
